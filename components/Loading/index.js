import React from "react";

// Styling
import { View, Spinner } from "native-base";

const Loading = () => {
  return (
    <View>
      <Spinner color="gray" />
    </View>
  );
};

export default Loading;
